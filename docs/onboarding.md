# Onboarding

1. Install package references for `Confluent.Kafka` and EnterpriseKafka projects.
2. Register `AddEnterpriseKafka` in startup.
3. Implement `IKafkaHandler<T>` in service projects.
4. Configure topics using `[KafkaTopic]` or appsettings mappings.
